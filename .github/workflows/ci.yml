name: CI Pipeline
on:
  push:
    branches-ignore:
      - 'main'
    tags-ignore:
      - 'v*'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Format and Lint
        run: npx @biomejs/biome check

      - name: Type Check
        run: npx tsc --noEmit

      - name: Debug Media Chrome Folder Structure
        run: |
          ls -R node_modules/media-chrome/react || echo "react directory not found"
          cat node_modules/media-chrome/package.json | grep -A 20 '"exports"' || true

      - name: Test
        run: npm test